apiVersion: v1
kind: ConfigMap
metadata:
  name: tripmind-config
data:
  # API Service
  jwt-expire: "7d"
  frontend-url: "https://tripmind.com"
  google-callback-url: "https://api.tripmind.com/api/v1/auth/google/callback"

  # AI Service
  default-ai-model: "gemini-2.0-flash"
  ai-execution-mode: "actual"
